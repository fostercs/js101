const solution = (numbers) => {
    // Type your solution here
    let numLength = numbers.length;
    console.log("hello world ", numLength);
    //     const (largestNum) => {
    const greeting = "hello world";

    function largestNum(greeting) {
        console.log("largest number calculator // begin program", greeting);
        //         return greeting;
    }
    // call our program
    largestNum(greeting);
};
